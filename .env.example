# Database Configuration
DATABASE_URL=postgresql://evamp:your_secure_password_here@localhost:5432/evamp_ops
DB_USER=evamp
DB_PASSWORD=your_secure_password_here

# Encryption Key (Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your_encryption_key_here

# eBay API Credentials (from https://developer.ebay.com/my/keys)
EBAY_APP_ID=your_app_id_here
EBAY_CERT_ID=your_cert_id_here
EBAY_DEV_ID=your_dev_id_here
# RuName from eBay Developer Portal (User Tokens -> Redirect URL). Short string, not the full URL.
EBAY_REDIRECT_URI=your_runame_here
# Tunnel URL (e.g. https://xxx.localhost.run). Settings > eBay shows the full callback URL to paste in eBay.
CALLBACK_BASE_URL=
# Marketplace for Finances API (Order earnings). E.g. EBAY_GB, EBAY_US. Required for Sales Analytics net earnings.
EBAY_MARKETPLACE_ID=EBAY_GB
# Optional: your eBay seller username; used to label message sender as seller vs buyer in Message dashboard
# EBAY_SELLER_USERNAME=

# eBay Webhook Secret - NOT the same as User Token!
# - User Token: What you got when you logged into eBay and granted permissions (OAuth).
#   Used to call eBay APIs (orders, messages). Stored/managed by the app when we add eBay OAuth.
# - Webhook Secret: A secret YOU create when configuring the webhook endpoint in eBay Developer
#   Portal (Notifications). eBay signs webhook requests with it; we verify the signature.
#   Only needed when you set up real-time message notifications. Leave placeholder until then.
EBAY_WEBHOOK_SECRET=your_webhook_secret_here

# AI Provider API Keys (Set via Settings UI, these are fallbacks)
# ANTHROPIC_API_KEY=your_anthropic_key_here
# OPENAI_API_KEY=your_openai_key_here

# Sales Analytics profit: convert USD/EUR to GBP (SKU landed cost & postage are USD; order amounts may be EUR/USD)
USD_TO_GBP_RATE=0.79
EUR_TO_GBP_RATE=0.86
# Profit after tax: displayed profit = gross profit * (1 - PROFIT_TAX_RATE). E.g. 0.30 = 30% tax (take-home 70%)
PROFIT_TAX_RATE=0.30

# Application Settings
DEBUG=true
CORS_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Message sending (Phase 4-6): keep false until ready to test replying to real customers
ENABLE_MESSAGE_SENDING=false
