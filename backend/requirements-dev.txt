# Development and testing - install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
faker==20.1.0

# Security auditing (run locally: safety check, pip-audit)
safety==2.3.5
pip-audit==2.6.1
